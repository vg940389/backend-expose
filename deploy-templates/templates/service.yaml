apiVersion: v1
kind: Service
metadata:
  name: {{ include "backend-expose.fullname" . }}
  labels:
    {{- include "backend-expose.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backend-expose.selectorLabels" . | nindent 4 }}
