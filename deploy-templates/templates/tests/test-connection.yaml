apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-expose.fullname" . }}-test-connection"
  labels:
    {{- include "backend-expose.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-expose.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
